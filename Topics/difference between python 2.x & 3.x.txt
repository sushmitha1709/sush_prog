difference between python 2.x & 3.x:

1)Division operator	
					Output in Python 2.x 		Output in Python 3.x :
	
	print 7 / 5			1				1.4
 				
	print -7 / 5 			_2				-1.4
		
2)print function
	the print keyword in Python 2.x is replaced by the print() function in Python 3.x
	if we use parentheses in python 2.x then there is no issue but if we don’t use parentheses in python 3.x, we get SyntaxError.
	
				print 'Hello, Geeks'
	
									print('Hope You like these facts')
	
3)Unicode
	In Python 2, an implicit str type is ASCII. But in Python 3.x implicit str type is Unicode
. 
	in Python 2.x (Bytes is same as str)			in Python 3.x (Bytes and str are different)
	
	in Python 2.x (Unicode and str are different)		in Python 3.x (Unicode and str are same)
	
	print(type('default string '))	<type 'str'>			<class 'str'>
 
	print(type(b'string with b '))	<type 'str'>			<class 'bytes'>
	
	print(type(u'string with b '))	<type 'unicode'>		<class 'str'>

4)xrange


	5)Error Handling
	6)_future_ module





